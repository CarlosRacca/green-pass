import React from "react";
import EmptyView from "../components/EmptyView";

const MisDatos = () => {
  return <EmptyView message="Todavía no cargaste tus datos personales." backLabel="Volver al panel" />;
};

export default MisDatos;
